var MyTable = React.createClass({    render: function() {      var tableRows = [];      this.state.items.forEach(function(item) {        tableRows.push(<tableRow item={item} onRemove={this.remove}/>);      }.bind(this));      return (        <div>          <table>            <MovieTableHeader />            <tbody>              {tableRows}            </tbody>          </table>          <button onClick={this.create}>Neu</button>        </div>      );    }});